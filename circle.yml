machine:
  timezone:
    America/New_York
  php:
    version: '5.6.14'

dependencies:
  pre:
    - echo 'alias php5="~/.phpenv/shims/php"' >> ~/.bashrc
    - echo 'alias php5-fpm="~/.phpenv/shims/php"' >> ~/.bashrc
    - source ~/.bashrc

test:
  override:
    - case $CIRCLE_NODE_INDEX in 0) sudo bash install.sh --server-webserver=apache ;; 1) sudo bash install.sh --server-webserver=nginx ;; esac:
        parallel: true

notify:
  webhooks:
    # A list of hook hashes, containing the url field
    # gitter hook
    - url: https://webhooks.gitter.im/e/70d4223b45d445ea23b3
