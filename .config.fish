# Helpful abbreviations for fish shell.
# See https://gist.github.com/jmolivas/df86020c41efb986b29b for a nice fish/oh my fish/setup.
# Load with:
#   source .config.fish
abbr -a -- c composer
abbr -a -- d docker
abbr -a -- dc docker-compose
abbr -a -- g git
abbr -a -- gd git diff
abbr -a -- gs git status
abbr -a -- gc git commit
abbr -a -- gco git checkout
abbr -a -- gp git push
abbr -a -- gpl git pull
abbr -a -- r bin/robo
