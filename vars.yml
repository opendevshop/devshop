# Vars file.
# The devshop.server playbook includes this file.
#
---

# The default version of PHP to use.
# We are stuck at the moment. PHP versions are no longer installable on ubuntu 18 as of EOL.
php_version: "8.0"
